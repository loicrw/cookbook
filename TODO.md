This markdown contains a list of tasks and ideas that can be picked up. It can be used as a backlog. Add your own execution plan/todos below the ---, don't touch the text above the ---. After you think you are done, run `npm check` to verify there are no errors in the code and that the tests still pass. Be sure to write tests for every item you change and update the documentation when applicable. When finished, write the relevant changes to the REVIEW.md file

# 1. update github pages publishing workflow

# 2. add help section explaining everything

# 3. Fix import and export buttons
In the settings tab under the data header there are two buttons ("Import recipes" and "Export recipes"). They take up quite a lot of space as they are right now. Currently they are stacked vertically. Ensure that they are next to each other on the same row.

---
